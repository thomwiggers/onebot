{
  "enabledPlugins": {
    "pyright-lsp@claude-plugins-official": true
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit:*)",
            "command": "uv run ruff format . && uv run ruff check --fix . && uv run ruff check .",
            "statusMessage": "Running ruff format + check before commit..."
          }
        ]
      }
    ]
  }
}
